namespace VocabularyBuilder.Domain.Enums;

/// <summary>
/// Represents supported languages in the vocabulary builder.
/// Each language has its own frequency data, parsing rules, and lemma grouping logic.
/// </summary>
public enum Language
{
    English = 0,
    French = 1
}
